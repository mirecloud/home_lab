apiVersion: v2
name: redis-wrapper
description: Wrapper chart pour Redis MireCloud
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: redis
    version: "2.1.0"
    repository: https://charts.pascaliske.dev